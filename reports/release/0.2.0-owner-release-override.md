# pyHermiT 0.2.0 owner release override

Date: 2026-08-02

The repository owner explicitly authorizes the `0.2.0` production release and
accepts the remaining external release risks recorded for pyHermiT. This release
migrates the shared ontology contract to `pyowl-core>=0.2,<0.3`, core model schema
2, wire format 1.2, and encoded structural-view schema 2. It does not change the
project license or the retained source-guided provenance boundary.

`LIC-001` is **waived as-is** for this release. The waiver is an owner release
decision, not legal advice, not an owner/legal-review signoff, and not a
representation that legal review occurred. The LGPL-3.0-or-later project
decision, provenance inventory, notices, corresponding-source policy, and
completed repository audits remain unchanged.

The unavailable licensed W3C body run, larger Java differential sample, and
dedicated performance calibration are accepted as post-release follow-up work.
Publication remains authorized only after the forced-native, pure, artifact,
manifest, and attestation jobs verify the complete same-version candidate.

This record applies only to `pyHermiT 0.2.0`. It does not waive future release
gates, certify an unexecuted hosted target, or rewrite historical evidence.
