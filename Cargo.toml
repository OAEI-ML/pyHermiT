[workspace]
members = []
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.83"
license = "LGPL-3.0-or-later"
repository = "https://github.com/OAEI-ML/pyHermiT"

# The publish-disabled native crate remains its own workspace so optional PEP 517 builds
# consume `native/Cargo.lock` directly. This root manifest is the repository-wide metadata
# anchor; release profile settings live in the crate that actually produces the extension.
